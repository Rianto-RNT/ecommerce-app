import { ShopRntValidators } from './shop-rnt-validators';

describe('ShopRntValidators', () => {
  it('should create an instance', () => {
    expect(new ShopRntValidators()).toBeTruthy();
  });
});
